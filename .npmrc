# Prefer local workspace packages (good for monorepos)
save-workspace-protocol=true

# Avoid version drift
save-exact=true

# Avoid package-lock.json when using Bun
package-lock=false

# Use npm registry
registry=https://registry.npmjs.org/

# Reduce noise
loglevel=warn